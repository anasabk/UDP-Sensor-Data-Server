server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    password: 1236760
    url: jdbc:postgresql://localhost:5432/iot_data
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
#    show-sql: true

  udp:
    channel: inboundChannel
    port: 8080